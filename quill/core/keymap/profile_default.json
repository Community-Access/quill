{
  "_name": "QUILL Default",
  "_description": "Full QUILL keyboard layout. Recommended starting point for most users.",
  "_base": "default",
  "_comment": "The default profile is DEFAULT_KEYMAP itself, so its delta is empty and stays empty: choosing it means 'give me the shipped layout'. It used to be a 184-binding snapshot that had fallen 29 bindings behind the defaults it is named after (bad.md P2.6).",
  "bindings": {}
}
