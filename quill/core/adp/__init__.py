"""ADP Assistant (Audio Description Project) integration.

Pre-release: the ADP Assistant (``future.adp_assistant``) is un-gated for
testing -- available by default -- while hands-free ADP Voice Mode
(``future.adp_voice_mode``) stays ``locked_off`` and reachable only via a
signed unlock code (Help > Redeem Unlock Code...). Deliberately undocumented
in user-facing surfaces until public launch.
"""

from __future__ import annotations
